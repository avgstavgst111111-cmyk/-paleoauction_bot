"""
🪙 Telegram бот для распознавания монет с поиском на Виолити
"""

import os
import sys
from io import BytesIO
import base64
import re
from statistics import mean, median

# Проверка и установка библиотек
print("🔧 Проверка необходимых библиотек...")
try:
    from telegram import Update
    from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes
    print("✅ python-telegram-bot установлен")
except ImportError:
    print("📦 Установка python-telegram-bot...")
    os.system("pip install python-telegram-bot --quiet")
    from telegram import Update
    from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes

try:
    import requests
    from bs4 import BeautifulSoup
    print("✅ requests и beautifulsoup4 установлены")
except ImportError:
    print("📦 Установка requests и beautifulsoup4...")
    os.system("pip install requests beautifulsoup4 --quiet")
    import requests
    from bs4 import BeautifulSoup

try:
    from openai import OpenAI
    print("✅ openai установлен")
except ImportError:
    print("📦 Установка openai...")
    os.system("pip install openai --quiet")
    from openai import OpenAI

print("\n" + "="*50)
print("🪙 TELEGRAM БОТ ДЛЯ РАСПОЗНАВАНИЯ МОНЕТ")
print("="*50 + "\n")

# ============================================
# НАСТРОЙКИ
# ============================================
# Берём ключи из переменных окружения (безопасно) или из кода (для разработки)
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "8209002461:AAGK-9tWbvOUhkC3mcZ0wk0DLTDKVIH_jWU")
OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "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")

# Проверка токенов
if not TELEGRAM_BOT_TOKEN:
    print("❌ ОШИБКА: Не указан TELEGRAM_BOT_TOKEN")
    sys.exit(1)

if not OPENAI_API_KEY:
    print("❌ ОШИБКА: Не указан OPENAI_API_KEY")
    sys.exit(1)

print("✅ Все ключи подключены")

# Инициализация OpenAI
client = OpenAI(api_key=OPENAI_API_KEY)


# ============================================
# ФУНКЦИИ ПОИСКА НА ВИОЛИТИ
# ============================================

def search_violity(coin_description):
    """Ищет монеты на Виолити и возвращает средние цены"""
    try:
        # Формируем поисковый запрос
        search_query = coin_description.replace(" ", "+")
        url = f"https://violity.com/search?search_text={search_query}&category_id=5"
        
        headers = {
            'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
        }
        
        response = requests.get(url, headers=headers, timeout=10)
        
        if response.status_code != 200:
            return None
        
        soup = BeautifulSoup(response.text, 'html.parser')
        
        # Ищем лоты и цены
        prices = []
        lots = []
        
        # Поиск цен (Виолити использует разные классы для цен)
        price_elements = soup.find_all(['span', 'div'], class_=re.compile('price|cost|value', re.I))
        
        for elem in price_elements:
            text = elem.get_text()
            # Ищем числа с грн или UAH
            numbers = re.findall(r'(\d+[\s,]?\d*)\s*(?:грн|UAH|₴)', text)
            for num in numbers:
                try:
                    price = float(num.replace(',', '.').replace(' ', ''))
                    if 1 <= price <= 1000000:  # Разумные границы цены
                        prices.append(price)
                except:
                    continue
        
        # Ищем информацию о лотах
        lot_elements = soup.find_all(['div', 'a'], class_=re.compile('lot|item|product', re.I))
        for lot in lot_elements[:10]:  # Берём первые 10 лотов
            title = lot.get_text().strip()
            if title and len(title) > 10:
                lots.append(title[:100])  # Ограничиваем длину
        
        if not prices:
            return {
                'found': False,
                'message': 'Не найдено активных лотов с ценами на Виолити'
            }
        
        # Вычисляем статистику
        avg_price = mean(prices)
        med_price = median(prices)
        min_price = min(prices)
        max_price = max(prices)
        
        return {
            'found': True,
            'count': len(prices),
            'average': avg_price,
            'median': med_price,
            'min': min_price,
            'max': max_price,
            'lots': lots[:5],  # Первые 5 лотов
            'url': url
        }
        
    except Exception as e:
        print(f"Ошибка поиска на Виолити: {e}")
        return {
            'found': False,
            'message': f'Ошибка поиска: {str(e)}'
        }


def format_violity_results(results, coin_name):
    """Форматирует результаты поиска на Виолити"""
    if not results or not results.get('found'):
        return f"""

🔍 **ПОИСК НА ВИОЛИТИ:**
К сожалению, не найдено активных лотов для "{coin_name}"

💡 Попробуйте:
• Уточнить описание монеты
• Поискать вручную на [Violity.com](https://violity.com)"""
    
    text = f"""

💰 **ЦЕНЫ НА ВИОЛИТИ** (найдено {results['count']} лотов):

📊 **Средняя цена:** {results['average']:.2f} грн
📈 **Медианная цена:** {results['median']:.2f} грн
🔽 **Минимум:** {results['min']:.2f} грн
🔼 **Максимум:** {results['max']:.2f} грн

🔗 [Посмотреть все лоты на Виолити]({results['url']})
"""
    
    if results.get('lots'):
        text += "\n📦 **Примеры найденных лотов:**\n"
        for i, lot in enumerate(results['lots'], 1):
            text += f"{i}. {lot}\n"
    
    return text


# ============================================
# ФУНКЦИИ РАСПОЗНАВАНИЯ
# ============================================

async def recognize_coin_with_openai(image_bytes):
    """Распознаёт монету используя GPT-4 Vision"""
    try:
        # Конвертируем изображение в base64
        base64_image = base64.b64encode(image_bytes).decode('utf-8')
        
        # Отправляем запрос к OpenAI
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {
                    "role": "user",
                    "content": [
                        {
                            "type": "text",
                            "text": """Проанализируй это изображение монеты и предоставь информацию на русском языке:

1. **Страна:** Страна происхождения
2. **Номинал:** Достоинство (1 рубль, 50 копеек, 1 доллар и т.д.)
3. **Год выпуска:** Если виден
4. **Материал:** Из чего сделана
5. **Описание:** Что изображено на монете
6. **Коллекционная ценность:** Если известно

В конце напиши краткое название монеты для поиска (например: "1 рубль 1997 СПМД" или "50 копеек СССР 1961")

Отвечай кратко и структурированно."""
                        },
                        {
                            "type": "image_url",
                            "image_url": {
                                "url": f"data:image/jpeg;base64,{base64_image}"
                            }
                        }
                    ]
                }
            ],
            max_tokens=1000
        )
        
        return response.choices[0].message.content
    
    except Exception as e:
        error_msg = str(e)
        if "insufficient_quota" in error_msg or "quota" in error_msg.lower():
            return """❌ Превышен лимит OpenAI API

К сожалению, закончился кредит OpenAI.

Опишите монету текстом, и я найду её на Виолити!"""
        else:
            return f"❌ Ошибка: {error_msg}"


def extract_coin_name(ai_response):
    """Извлекает название монеты из ответа AI для поиска"""
    # Ищем последнюю строку или строку с ключевыми словами
    lines = ai_response.split('\n')
    
    # Ищем строки с ключевыми словами
    for line in reversed(lines):
        if any(word in line.lower() for word in ['поиск', 'название', 'для поиска', 'монета']):
            # Удаляем лишнее
            clean = re.sub(r'для поиска:|название:|монета:', '', line, flags=re.I).strip()
            if len(clean) > 3:
                return clean
    
    # Пытаемся найти год и номинал
    year_match = re.search(r'\b(19|20)\d{2}\b', ai_response)
    nominal_match = re.search(r'\d+\s*(рубл|копе|гривн|долар|евро|cent)', ai_response, re.I)
    
    if year_match and nominal_match:
        return f"{nominal_match.group(0)} {year_match.group(0)}"
    
    # Возвращаем первые значимые слова
    words = [w for w in ai_response.split() if len(w) > 3]
    return ' '.join(words[:5]) if words else "монета"


# ============================================
# ОБРАБОТЧИКИ КОМАНД
# ============================================

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Приветствие"""
    welcome_text = """🪙 **Бот для распознавания и оценки монет!**

**Что я умею:**
• 🔍 Распознаю монеты по фото (AI GPT-4)
• 💰 Ищу цены на **Виолити**
• 📊 Показываю среднюю цену
• 📈 Даю статистику по лотам

**Как пользоваться:**
Просто отправьте **чёткое фото монеты**!

💡 **Советы:**
✓ Хорошее освещение
✓ Монета в фокусе
✓ Контрастный фон
✓ Можно обе стороны по очереди

Или опишите монету текстом (например: "1 рубль 1997")

Готовы? Отправляйте фото! 📸"""
    
    await update.message.reply_text(welcome_text, parse_mode='Markdown')


async def help_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Справка"""
    help_text = """ℹ️ **СПРАВКА**

**Команды:**
/start - Начать
/help - Справка

**Как работает:**
1. 📸 Вы отправляете фото монеты
2. 🤖 AI анализирует изображение
3. 🔍 Бот ищет на Виолити
4. 💰 Показывает среднюю цену

**Или опишите текстом:**
"1 рубль 1997 СПМД"
"50 копеек СССР 1961"

**Что определяется:**
✓ Страна, год, номинал
✓ Материал и описание
✓ Цены на Виолити
✓ Средняя/мин/макс цена
✓ Количество активных лотов

Отправьте фото и попробуйте! 🪙"""
    
    await update.message.reply_text(help_text, parse_mode='Markdown')


async def handle_photo(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработка фотографий"""
    processing_msg = await update.message.reply_text(
        "🔍 **Анализирую монету...**\n⏳ Подождите 10-15 секунд"
    )
    
    try:
        # Получаем фото
        photo = update.message.photo[-1]
        photo_file = await photo.get_file()
        photo_bytes = await photo_file.download_as_bytearray()
        
        # Анализируем с помощью AI
        await processing_msg.edit_text("🤖 AI анализирует изображение...")
        analysis = await recognize_coin_with_openai(bytes(photo_bytes))
        
        # Извлекаем название для поиска
        coin_name = extract_coin_name(analysis)
        
        # Ищем на Виолити
        await processing_msg.edit_text(f"💰 Ищу цены на Виолити для: {coin_name}...")
        violity_results = search_violity(coin_name)
        
        # Форматируем результаты
        violity_text = format_violity_results(violity_results, coin_name)
        
        # Итоговый ответ
        response = f"""🪙 **РЕЗУЛЬТАТ АНАЛИЗА**

{analysis}
{violity_text}

---

💡 **Дополнительные ресурсы:**
• [Meshok.net](https://meshok.net) - нумизматика
• [Auction.ru](https://auction.ru) - аукционы

🔄 Хотите проверить другую сторону? Отправьте ещё фото!"""
        
        await processing_msg.delete()
        await update.message.reply_text(response, parse_mode='Markdown', disable_web_page_preview=True)
        
    except Exception as e:
        await processing_msg.delete()
        await update.message.reply_text(
            f"❌ **Ошибка:** {str(e)}\n\nПопробуйте другое фото или опишите монету текстом",
            parse_mode='Markdown'
        )


async def handle_text(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обработка текста"""
    user_text = update.message.text
    
    processing_msg = await update.message.reply_text(
        f"🔍 Ищу '{user_text}' на Виолити..."
    )
    
    try:
        # Ищем на Виолити
        violity_results = search_violity(user_text)
        violity_text = format_violity_results(violity_results, user_text)
        
        response = f"""🪙 **Поиск: "{user_text}"**
{violity_text}

💡 **Совет:** Отправьте фото монеты для точного AI-анализа!

**Другие сайты:**
• [Meshok.net](https://meshok.net/numizmatika)
• [Auction.ru](https://auction.ru)"""
        
        await processing_msg.delete()
        await update.message.reply_text(response, parse_mode='Markdown', disable_web_page_preview=True)
        
    except Exception as e:
        await processing_msg.delete()
        await update.message.reply_text(
            f"❌ Ошибка поиска: {str(e)}",
            parse_mode='Markdown'
        )


# ============================================
# ЗАПУСК
# ============================================

def main():
    """Запуск бота"""
    print("🚀 Запуск бота...")
    
    try:
        application = Application.builder().token(TELEGRAM_BOT_TOKEN).build()
        
        application.add_handler(CommandHandler("start", start))
        application.add_handler(CommandHandler("help", help_command))
        application.add_handler(MessageHandler(filters.PHOTO, handle_photo))
        application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_text))
        
        print("\n" + "="*50)
        print("✅ БОТ ЗАПУЩЕН!")
        print("="*50)
        print(f"\n📱 @paleoauction_bot")
        print("🤖 AI: OpenAI GPT-4 Vision")
        print("💰 Поиск: Violity.com")
        print("⏹️  Ctrl+C для остановки\n")
        
        application.run_polling(allowed_updates=Update.ALL_TYPES)
        
    except Exception as e:
        print(f"\n❌ ОШИБКА: {e}")
        sys.exit(1)


if __name__ == '__main__':
    main()
python-telegram-bot==20.7
openai==1.3.0
requests==2.31.0
beautifulsoup4==4.12.2
Pillow==10.1.0
